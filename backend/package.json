{
  "name": "auth-injector",
  "version": "1.0.0",
  "description": "Express proxy that injects an auth header into forwarded requests and replays the upstream response verbatim.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "http-proxy-middleware": "^3.0.3"
  }
}
